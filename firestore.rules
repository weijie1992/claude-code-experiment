rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TEST MODE: allow all reads and writes until 2026-04-28
      allow read, write: if request.time < timestamp.date(2026, 4, 28);
    }
  }
}
